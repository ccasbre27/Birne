<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage x:Class="Birne.MobileApp.View.HomePageDetail"
            xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:android="clr-namespace:Xamarin.Forms.PlatformConfiguration.AndroidSpecific;assembly=Xamarin.Forms.Core"
            android:TabbedPage.ToolbarPlacement="Bottom"
            BarBackgroundColor="{StaticResource MainColor}"
            SelectedTabColor="White">
    <ContentPage ControlTemplate="{StaticResource ctm-base}"
                 IconImageSource="home_icon.png">
        <ContentPage.Content>
            <StackLayout Spacing="20">
                <Frame BackgroundColor="#D8D8D8"
                       CornerRadius="25"
                       HeightRequest="150" />

                <Label Style="{StaticResource lblTitle}"
                       Text="Buscar tus productos" />

                <SearchBar Placeholder="Search items..." />
            </StackLayout>
        </ContentPage.Content>
    </ContentPage>
    <ContentPage ControlTemplate="{StaticResource ctm-base}"
                 IconImageSource="buy_icon.png" />

    <ContentPage ControlTemplate="{StaticResource ctm-base}"
                 IconImageSource="user_icon.png" />

</TabbedPage>
